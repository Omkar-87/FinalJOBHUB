# ── Server ──
server.port=8080

# ── PostgreSQL Database ──
spring.datasource.url=jdbc:postgresql://localhost:5432/JobHub_AI
spring.datasource.username=postgres
spring.datasource.password=Omkar@007
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── Mail (required by EmailService) ──
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=omkarchoudhuary@gmail.com
spring.mail.password=Omkar@007
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ── App-specific (used by EmailService for reset links) ──
app.frontend-url=http://localhost:3000